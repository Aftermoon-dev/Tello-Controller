package dev.aftermoon.tellocontroller

import java.lang.Math.abs

class Utils {
    /**
     * Azimuth List를 이용하여 스마트폰 회전 감지
     * @param azimuthList Azimuth ArrayList
     * @return 0 - 회전 없음 or 정보 부족 / 1 - 왼쪽 / 2 - 오른쪽
     */
//    fun calRotation(azimuthList: ArrayList<Double>): Int {
//        val max = azimuthList.maxOrNull()
//        val min = azimuthList.minOrNull()
//
//        if (abs(max) - abs(min) > )
//    }
}